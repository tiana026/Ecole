@model IEnumerable<Ecole.Models.Matiere>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Enseignant.NomComplet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade.Promotion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intitule)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Enseignant.NomComplet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade.Promotion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intitule)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
